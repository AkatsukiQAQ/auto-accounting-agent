# Dev log — 2026-04-22 evening

Session wrap-up for the MITA Finance refactor.

## What shipped tonight

Three commits on branch `feature/basic_frontend`:

1. **`docs: add MITA Finance refactor design docs`** — imported the `design/` folder (HANDOFF, per-phase specs, interactive React prototype under `design-references/`).
2. **`build: migrate Python tooling from pip+conda to uv`** — `pyproject.toml` + `uv.lock` are SSOT; `requirements.txt` deleted; README install section rewritten. `.venv/` is the PyCharm interpreter.
3. **`feat(backend): extract framework-free core/ from legacy LangChain tools`** — BACKEND_PHASE_0. `backend/core/` (8 modules, zero langchain imports) + `backend/tests/core/` (62 passing tests). Three latent bugs in legacy `receipt_classifier.py` fixed in the new port; legacy `backend/tools/` untouched.

## Task board state (as of this commit)

| # | Phase | Status | Notes |
|---|---|---|---|
| 4 | BACKEND_PHASE_0 — extract core/ pure functions | **completed** | 62/62 tests green under `uv run pytest`. |
| 5 | BACKEND_PHASE_1 — FastAPI + DB + ImportPipeline | pending (unblocked) | **Next up.** 3–4 day scope. |
| 7 | PHASE_1 (frontend) — wire 5 screens to real backend | pending (blocked by #5) | |
| 2 | Dogfood P1 end-to-end before P2 | pending (blocked by #7) | |
| 1 | BACKEND_PHASE_2 — ledger, cascades, workers | pending (blocked by #2) | |
| 6 | PHASE_2 (frontend) — accounts / budget / recurring / review | pending (blocked by #1) | |
| 3 | PHASE_3 backlog — pick one feature at a time | pending (blocked by #6) | Menu, not a sprint. |

Dependency chain: #4 → #5 → #7 → #2 → #1 → #6 → #3.

## Environment state

- Python 3.11 via uv. `uv sync --dev` from a clean clone rebuilds `.venv/` deterministically from `uv.lock`.
- Backend lib deps: langchain / langchain-openai / langgraph / openai / pydantic / python-dotenv / PyYAML. Dev group: pytest.
- `[project.optional-dependencies].core-only` in `pyproject.toml` = `{pydantic, PyYAML}` — handy for auditing that `backend/core/` stayed framework-free.
- Legacy `backend/tools/` still runs against the full dep set; `test.py` demo unchanged.

## Open items (not in this push — for context)

- **Frontend WIP is still uncommitted** on this branch: `frontend/src/components/app/TopBar.tsx` (new), `receiptCard.tsx` (deleted), various i18n/tsconfig tweaks, and root-level `package.json` duplication. Pre-existing work, not tonight's scope — user to decide whether to commit or redo on a separate branch.
- **PyCharm interpreter swap** is manual: `File → Settings → Project → Python Interpreter → Add → Existing → .venv/Scripts/python.exe`.
- **`.env` not committed** (contains OpenAI key); unchanged.

## What BACKEND_PHASE_1 will need

(From `design/BACKEND_PHASE_1.md`, reminder for next session.)

- FastAPI + SQLAlchemy + Alembic.
- Two tables only: `transactions`, `categories`. No accounts / budgets / merchants yet.
- Five REST groups: `/api/transactions`, `/api/categories`, `/api/import/photo`, `/api/settings`, `/healthz`.
- New `ImportPipeline` orchestrator (deterministic stages) replacing `create_react_agent`.
- `PipelineLLM` interface — one LLM client wrapping `ChatOpenAI`, model name in config.
- Seeder that writes 9–10 default categories from `load_seed_categories()` on first boot.
- OpenAPI docs at `/docs`.
- Single-user, no auth.

Estimated effort: 3–4 days.

## Resume protocol

1. Tomorrow, after any office-side work, say "pull / fetch" — I'll sync this branch.
2. I'll re-run `uv sync --dev` + `uv run pytest backend/tests/core/` to confirm the env is reproducible on that machine.
3. I'll refresh the task list from `TaskList` and reconcile against this file if anything changed.
4. Then we enter plan mode for BACKEND_PHASE_1.
