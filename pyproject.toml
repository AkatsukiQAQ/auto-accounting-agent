[project]
name = "auto-accounting-agent"
version = "0.1.0"
description = "MITA Finance — LangGraph-powered receipt OCR + classification, in transition from MVP pipeline to FastAPI service."
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = { text = "MIT" }
dependencies = [
    "alembic>=1.13",
    "fastapi>=0.115",
    "langchain>=0.3",
    "langchain-core>=0.3",
    "langchain-openai>=0.3",
    "langgraph>=0.6",
    "openai>=2.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.5",
    "python-dotenv>=1.0",
    "python-multipart>=0.0.9",
    "PyYAML>=6.0",
    "sqlalchemy>=2.0",
    "uvicorn[standard]>=0.30",
]

[project.optional-dependencies]
# The minimal set that backend/core/ depends on. Verifies the framework-free
# invariant of Phase 0: `uv pip install --extra core-only` installs only these
# two — anything else indicates backend/core/ leaked a legacy dependency.
core-only = [
    "pydantic>=2.0",
    "PyYAML>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend"]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]

[tool.uv]
# Default group(s) installed when running `uv sync` with no flags.
default-groups = ["dev"]
